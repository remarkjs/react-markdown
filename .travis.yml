language: node_js
node_js: node
deploy:
  provider: pages
  local_dir: dest
  target_branch: gh-pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  email: tituswormer@gmail.com
  name: Titus Wormer
  on:
    branch: website
